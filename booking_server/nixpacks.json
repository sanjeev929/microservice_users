{
    "phases": {
      "setup": {
        "commands": [
          "python3 -m venv --copies /opt/venv",
          ". /opt/venv/bin/activate"
        ]
      },
      "install": {
        "commands": [
          "pip install -r requirements.txt"
        ]
      },
      "start": {
        "commands": [
          "gunicorn booking_server.wsgi:application"
        ]
      }
    },
    "environment": {
      "DJANGO_SETTINGS_MODULE": "booking_server.settings"
    }
  }
  